# -*- coding: utf-8 -*-
"""
阶段 1 · 总第 3 周 · D4 自查测验
规则:限时 1 小时,不看资料、不查文档,凭记忆写。
每题 1 分,共 10 题,≥8 分通过(可提前进阶段 2)。
写完对照 check_quiz_answers.py 自评,把卡壳的题记进错题清单(D5 处理)。

数据已经给好,直接往下写就行。做题顺序建议:按编号来,卡住超过 10 分钟就跳下一题。
⚠️ 提示:数据里有猫腻,注意看 Q4 对应的列。
"""
import pandas as pd

# ---------- 数据(直接用,别改) ----------
orders = pd.DataFrame({
    "order_id": [101, 102, 103, 104, 105, 106, 107, 108, 109, 110, 111, 112, 102, 114],
    "user_id":  [1, 2, 1, 3, 4, 2, 5, 1, 4, 6, 2, 3, 2, 7],
    "date":     ["2026-01-03", "2026-01-05", "2026-01-09", "2026-01-12", "2026-01-15",
                 "2026-02-02", "2026-02-07", "2026-02-11", "2026-02-14", "2026-02-18",
                 "2026-02-22", "2026-02-25", "2026-01-05", "2026-02-28"],
    "region":   ["华东", "华南", "华东", "华北", "华南", "华东", "华北", "华南", "华东", "华北", "华南", "华北", "华南", "华东"],
    "category": ["手机", "家电", "手机", "服饰", "家电", "手机", "服饰", "家电", "手机", "服饰", "手机", "家电", "家电", "服饰"],
    "quantity": [2, 1, None, 3, 1, 2, 4, 1, 2, None, 1, 3, 1, 2],
    "price":    ["1999", "4599", "2699", "129", "1299", "2199", "99", "4599", "2699", "199", "1999", "1599", "4599", "299"],
})

users = pd.DataFrame({
    "user_id": [1, 2, 3, 4, 5, 7],
    "name":    ["Alice", "Bob", "Cathy", "David", "Eva", "Fiona"],
    "city":    ["上海", "广州", "北京", "深圳", "杭州", "上海"],
    "level":   ["VIP", "普通", "普通", "VIP", "普通", "普通"],
})

# ---------- 开始答题(每题在下面写你的代码) ----------

# Q1. 打印 orders 的形状、所有列名,以及每列缺失值个数。
#     提示:shape / columns / isna().sum()

# Q2. 类型转换:把 date 转成 datetime,把 price 转成 float,并存回原列。
#     提示:pd.to_datetime / astype

# Q3. 计算每笔订单金额 = price * quantity,新增一列 total。

# Q4. 删除 orders 里完全重复的行(保留第一条)。做完这题后,让后面的题都基于去重后的 orders。
#     提示:先自己检查有没有重复,再 drop_duplicates

# Q5. 筛选 + 选列:找出 region == "华南" 且 total > 1000 的订单,只看 order_id 和 total 两列。
#     提示:布尔条件用 & 而不是 and

# Q6. 填充缺失值:quantity 为空的行,用"同一 category 的均价(向下取整)"填充。
#     提示:groupby('category').transform('mean') 取均值 → np.floor 向下取整 → fillna
#     (需要的话 import numpy as np)

# Q7. 分组聚合:按 region 分组,统计每个地区的 订单数、总金额、平均单价(price 的均值)。
#     提示:groupby + agg,或 groupby 后分别 count / sum / mean

# Q8. 多列分组:按 region × category 分组,输出每个组合的总金额,只保留 total 非空的组合。

# Q9. 时间聚合:把订单按"月份"聚合,统计每个月的订单总金额。
#     提示:dt.to_period('M') 或 resample

# Q10. 连接:orders 左连接 users(按 user_id),统计每个 city 的下单用户数(去重)和订单总金额。
#      再找出"在 users 里不存在"的 user_id(孤儿订单)。
#      提示:merge(how='left') / groupby / nunique / isin
